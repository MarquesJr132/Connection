package com.example.connection.service;

import com.example.connection.model.Account;
import com.example.connection.model.SignUp;

public interface AccountService {
    boolean saveCliente(Account cl);
}
